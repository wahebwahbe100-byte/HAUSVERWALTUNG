<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>BHV Immobilienverwaltung GmbH</title>
  <meta
    name="description"
    content="BHV Immobilienverwaltung GmbH – professionelle WEG-Verwaltung, Mietverwaltung und Sondereigentumsverwaltung."
  />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Inter:wght@400;500;600;700;800;900&display=swap"
    rel="stylesheet"
  />

  <style>
    :root {
      --blue: #247b91;
      --blue-dark: #1d6476;
      --dark: #243039;
      --text: #26333d;
      --muted: #6f7c84;
      --light: #f5f8f9;
      --white: #ffffff;
      --border: #dfe7ea;
      --shadow: 0 18px 45px rgba(29, 100, 118, 0.12);
      --radius: 20px;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: Inter, Arial, sans-serif;
      color: var(--text);
      background: var(--white);
      line-height: 1.65;
    }


    @keyframes pageFadeIn {
      from {
        opacity: 0.94;
        transform: translateY(4px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    body {
      animation: pageFadeIn 0.28s ease-out both;
    }

    .smooth-reveal {
      opacity: 0.96;
      transform: translateY(8px);
      transition: opacity 0.28s ease-out, transform 0.28s ease-out;
      will-change: opacity, transform;
    }

    .smooth-reveal.is-visible {
      opacity: 1;
      transform: translateY(0);
    }

    @media (prefers-reduced-motion: reduce) {
      *,
      *::before,
      *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        scroll-behavior: auto !important;
        transition-duration: 0.01ms !important;
      }

      .smooth-reveal,
      .reveal-step {
        opacity: 1 !important;
        transform: none !important;
      }
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    img {
      display: block;
      max-width: 100%;
    }

    .container {
      width: min(1180px, calc(100% - 36px));
      margin: auto;
    }

    .topbar {
      background: var(--dark);
      color: var(--white);
      font-size: 14px;
      padding: 9px 0;
    }

    .topbar .container {
      display: flex;
      justify-content: flex-end;
      gap: 26px;
      flex-wrap: wrap;
    }

    .topbar span {
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }

    header {
      position: sticky;
      top: 0;
      z-index: 99;
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(14px);
      border-bottom: 1px solid rgba(223, 231, 234, 0.9);
    }

    .navbar {
      height: 88px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 24px;
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 14px;
    }

    .brand img {
      width: 126px;
      height: auto;
    }

    .brand-text strong {
      display: block;
      color: var(--dark);
      font-size: 20px;
      line-height: 1;
      letter-spacing: 0.04em;
    }

    .brand-text small {
      display: block;
      margin-top: 5px;
      color: var(--blue);
      font-size: 12px;
      font-weight: 800;
      letter-spacing: 0.08em;
    }

    nav {
      display: flex;
      align-items: center;
      gap: 24px;
      font-size: 14px;
      font-weight: 700;
    }

    nav a {
      position: relative;
      padding: 31px 0;
    }

    nav a::after {
      content: "";
      position: absolute;
      left: 0;
      bottom: 22px;
      width: 0;
      height: 2px;
      background: var(--blue);
      transition: 0.25s;
    }

    nav a:hover::after,
    nav a.active::after {
      width: 100%;
    }

    .menu-btn {
      display: none;
      width: 48px;
      height: 48px;
      border: 1px solid rgba(36, 123, 145, 0.16);
      border-radius: 16px;
      background: linear-gradient(135deg, #ffffff, #eef8fa);
      color: var(--dark);
      cursor: pointer;
      box-shadow: 0 10px 24px rgba(29, 100, 118, 0.12);
      transition: transform 0.22s ease, box-shadow 0.22s ease, background 0.22s ease;
      position: relative;
      overflow: hidden;
    }

    .menu-btn:hover {
      transform: translateY(-1px);
      box-shadow: 0 14px 30px rgba(29, 100, 118, 0.16);
    }

    .menu-btn:active {
      transform: scale(0.96);
    }

    .menu-icon,
    .menu-icon::before,
    .menu-icon::after {
      position: absolute;
      left: 50%;
      width: 22px;
      height: 2px;
      border-radius: 999px;
      background: var(--blue);
      transform: translateX(-50%);
      transition: transform 0.28s ease, opacity 0.2s ease, top 0.28s ease;
    }

    .menu-icon {
      top: 50%;
    }

    .menu-icon::before,
    .menu-icon::after {
      content: "";
    }

    .menu-icon::before {
      top: -7px;
    }

    .menu-icon::after {
      top: 7px;
    }

    .menu-btn.is-open {
      background: linear-gradient(135deg, var(--blue), #3a91a6);
      box-shadow: 0 16px 34px rgba(36, 123, 145, 0.24);
    }

    .menu-btn.is-open .menu-icon {
      background: transparent;
    }

    .menu-btn.is-open .menu-icon::before,
    .menu-btn.is-open .menu-icon::after {
      top: 0;
      background: var(--white);
    }

    .menu-btn.is-open .menu-icon::before {
      transform: translateX(-50%) rotate(45deg);
    }

    .menu-btn.is-open .menu-icon::after {
      transform: translateX(-50%) rotate(-45deg);
    }

    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border: 0;
      border-radius: 10px;
      padding: 14px 22px;
      font-weight: 800;
      cursor: pointer;
      transition: 0.25s;
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--blue), #3a91a6);
      color: var(--white);
      box-shadow: 0 12px 28px rgba(36, 123, 145, 0.25);
    }

    .btn-primary:hover {
      transform: translateY(-2px);
    }

    .btn-outline {
      border: 1px solid var(--blue);
      background: var(--white);
      color: var(--blue);
    }

    .btn-outline:hover {
      background: var(--blue);
      color: var(--white);
    }

    .hero {
      position: relative;
      min-height: 680px;
      display: flex;
      align-items: center;
      overflow: hidden;
      background:
        linear-gradient(
          90deg,
          rgba(255, 255, 255, 0.98) 0%,
          rgba(255, 255, 255, 0.9) 42%,
          rgba(255, 255, 255, 0.35) 68%,
          rgba(255, 255, 255, 0.1) 100%
        ),
        url("https://images.unsplash.com/photo-1600585154340-be6161a56a0c?auto=format&fit=crop&w=1800&q=80")
          center right / cover no-repeat;
    }

    .hero::after {
      content: "";
      position: absolute;
      right: 0;
      bottom: 0;
      left: 0;
      height: 130px;
      background: linear-gradient(0deg, #ffffff, transparent);
    }

    .hero-content {
      position: relative;
      z-index: 1;
      width: min(650px, 100%);
      padding: 88px 0;
    }

    .hero h1 {
      margin-bottom: 22px;
      color: var(--dark);
      font-size: clamp(40px, 6vw, 74px);
      line-height: 1.05;
      letter-spacing: -0.05em;
    }

    .hero h1 span {
      display: block;
      color: var(--blue);
    }

    .hero p {
      max-width: 560px;
      margin-bottom: 30px;
      color: #394952;
      font-size: 19px;
    }

    .hero-actions {
      display: flex;
      gap: 16px;
      flex-wrap: wrap;
    }

    section {
      padding: 88px 0;
    }

    .section-head {
      margin-bottom: 44px;
      text-align: center;
    }

    .eyebrow {
      margin-bottom: 9px;
      color: var(--blue);
      font-size: 13px;
      font-weight: 900;
      letter-spacing: 0.13em;
      text-transform: uppercase;
    }

    .section-head h2 {
      color: var(--dark);
      font-size: clamp(30px, 4vw, 46px);
      line-height: 1.15;
      letter-spacing: -0.035em;
    }

    .line {
      width: 48px;
      height: 3px;
      margin: 18px auto 0;
      border-radius: 10px;
      background: var(--blue);
    }

    .services {
      background: var(--light);
    }

    .cards {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 26px;
    }

    .card {
      padding: 34px;
      border: 1px solid var(--border);
      border-radius: var(--radius);
      background: var(--white);
      box-shadow: var(--shadow);
      transition: 0.25s;
    }

    .card:hover {
      transform: translateY(-8px);
    }

    .service-thumb {
      width: 100%;
      height: 210px;
      object-fit: cover;
      object-position: center;
      margin-bottom: 22px;
      border: 1px solid var(--border);
      border-radius: 16px;
      background: var(--light);
    }

    .card h3 {
      margin-bottom: 10px;
      color: var(--dark);
      font-size: 22px;
    }

    .card p {
      margin-bottom: 18px;
      color: #53616a;
    }

    .more {
      color: var(--blue);
      font-weight: 900;
    }

    .process-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 20px;
    }

    .step {
      padding: 28px;
      border: 1px solid var(--border);
      border-radius: var(--radius);
      background: var(--white);
    }

    .step-icon {
      width: 62px;
      height: 62px;
      object-fit: contain;
      display: block;
      margin-bottom: 16px;
    }

    .step h3 {
      margin-bottom: 8px;
      color: var(--dark);
    }


    .reveal-step {
      opacity: 0;
      transform: translateY(10px);
      transition: opacity 0.45s ease-out, transform 0.45s ease-out;
    }

    .reveal-step.is-visible {
      opacity: 1;
      transform: translateY(0);
    }

    .reveal-step:nth-child(2) {
      transition-delay: 0.08s;
    }

    .reveal-step:nth-child(3) {
      transition-delay: 0.16s;
    }

    .reveal-step:nth-child(4) {
      transition-delay: 0.24s;
    }

    .benefits {
      background: var(--light);
    }

    .benefit-grid {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 0;
      overflow: hidden;
      border: 1px solid var(--border);
      border-radius: 28px;
      background: var(--white);
      box-shadow: var(--shadow);
    }

    .benefit {
      display: grid;
      grid-template-columns: 78px 1fr;
      gap: 22px;
      align-items: start;
      min-height: 220px;
      padding: 34px 30px;
      background: var(--white);
      text-align: left;
      border-right: 1px solid var(--border);
    }

    .benefit:last-child {
      border-right: 0;
    }

    .benefit-thumb {
      width: 58px;
      height: 58px;
      object-fit: contain;
      object-position: center;
      margin: 2px 0 0;
      padding: 0;
      border-radius: 0;
      background: transparent;
    }

    .benefit-text {
      flex: 1;
      min-width: 0;
    }

    .benefit h3 {
      margin-bottom: 10px;
      color: var(--dark);
      font-size: 20px;
      line-height: 1.25;
      word-break: normal;
      overflow-wrap: normal;
      hyphens: none;
    }



    .benefit h3.small-title {
      font-size: 17px;
      letter-spacing: -0.02em;
    }

    .benefit p {
      color: #5f6b72;
      font-size: 15px;
      line-height: 1.75;
      word-break: normal;
      overflow-wrap: normal;
      hyphens: none;
    }


    .trust-strip {
      background: var(--dark);
      padding: 0;
    }

    .trust-strip-box {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      border-radius: 0;
      overflow: hidden;
      background: linear-gradient(135deg, var(--dark), #17232b);
      color: var(--white);
      box-shadow: var(--shadow);
    }

    .trust-item {
      display: grid;
      grid-template-columns: 78px 1fr;
      gap: 24px;
      align-items: center;
      min-height: 150px;
      padding: 34px 44px;
      border-right: 1px solid rgba(255, 255, 255, 0.16);
    }

    .trust-item:last-child {
      border-right: 0;
    }

    .trust-icon {
      width: 60px;
      height: 60px;
      object-fit: contain;
    }

    .trust-number {
      display: block;
      color: #9bd0dc;
      font-size: 42px;
      line-height: 1;
      font-weight: 900;
      letter-spacing: -0.04em;
      margin-bottom: 8px;
    }



    .count-up {
      font-variant-numeric: tabular-nums;
    }

    .trust-title {
      display: block;
      color: var(--white);
      font-size: 15px;
      font-weight: 900;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      margin-bottom: 6px;
    }

    .trust-item p {
      color: rgba(255, 255, 255, 0.78);
      font-size: 15px;
      line-height: 1.55;
    }

    .request {
      background: linear-gradient(135deg, #ffffff 0%, #edf7f9 100%);
    }

    .request-grid {
      display: grid;
      grid-template-columns: 0.9fr 1.3fr;
      gap: 48px;
      align-items: start;
    }

    .request-text h2 {
      margin-bottom: 18px;
      color: var(--dark);
      font-size: clamp(31px, 4vw, 46px);
      line-height: 1.12;
    }

    .contact-list {
      display: grid;
      gap: 14px;
      margin-top: 24px;
    }

    .contact-list div {
      display: flex;
      gap: 12px;
    }

    form {
      padding: 30px;
      border: 1px solid var(--border);
      border-radius: var(--radius);
      background: var(--white);
      box-shadow: var(--shadow);
    }

    .form-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 18px;
    }

    label {
      display: block;
      margin-bottom: 7px;
      color: #34434c;
      font-size: 14px;
      font-weight: 800;
    }

    input,
    textarea,
    select {
      width: 100%;
      border: 1px solid #d4e0e4;
      border-radius: 9px;
      padding: 13px 14px;
      font-family: inherit;
      font-size: 15px;
      outline: none;
    }

    input:focus,
    textarea:focus,
    select:focus {
      border-color: var(--blue);
      box-shadow: 0 0 0 4px rgba(36, 123, 145, 0.12);
    }

    textarea {
      min-height: 120px;
      resize: vertical;
    }

    .full {
      grid-column: 1 / -1;
    }

    form .btn {
      width: 100%;
      margin-top: 18px;
      font-size: 16px;
    }

    .about-panel {
      display: grid;
      grid-template-columns: 1.7fr 0.75fr;
      max-width: 1060px;
      margin: 0 auto;
      overflow: hidden;
      border: 1px solid var(--border);
      border-radius: 18px;
      background: var(--white);
      box-shadow: var(--shadow);
    }

    .about-image-side {
      min-height: 340px;
      display: flex;
      align-items: center;
      position: relative;
      padding: 40px 46px;
      background:
        linear-gradient(90deg, rgba(255,255,255,0.92), rgba(255,255,255,0.62)),
        url('https://images.unsplash.com/photo-1560518883-ce09059eeffa?auto=format&fit=crop&w=1500&q=80') center / cover no-repeat;
    }

    .about-image-side::after {
      content: "";
      position: absolute;
      inset: 0;
      background: rgba(36, 123, 145, 0.04);
      pointer-events: none;
    }

    .about-content {
      max-width: 620px;
      position: relative;
      z-index: 1;
    }

    .about-content h2 {
      margin-bottom: 14px;
      color: var(--dark);
      font-size: clamp(26px, 3vw, 36px);
      line-height: 1.12;
      letter-spacing: -0.025em;
      text-transform: uppercase;
    }

    .about-content p {
      max-width: 520px;
      margin-bottom: 10px;
      color: #34434c;
      font-size: 15px;
      line-height: 1.75;
    }

    .about-signature {
      display: inline-block;
      margin-top: 14px;
      color: var(--blue);
      font-family: "Great Vibes", cursive;
      font-size: 48px;
      font-weight: 400;
      line-height: 1;
      letter-spacing: 0.02em;
      text-transform: none;
    }

    .about-contact {
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 38px 34px;
      border-left: 1px solid var(--border);
      background: #fbfdfe;
    }

    .about-contact h3 {
      margin-bottom: 18px;
      color: var(--dark);
      font-size: 26px;
      line-height: 1.1;
      letter-spacing: 0.06em;
      text-transform: uppercase;
    }

    .about-contact-list {
      display: grid;
      gap: 12px;
      margin-bottom: 22px;
      color: #34434c;
      font-size: 15px;
      font-weight: 600;
    }

    .about-contact-list a,
    .about-contact-list span {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .about-contact-list b {
      width: 22px;
      color: var(--blue);
      font-size: 18px;
      line-height: 1;
    }

    footer {
      padding: 58px 0 22px;
      background: linear-gradient(135deg, var(--dark), #17232b);
      color: var(--white);
    }

    .footer-grid {
      display: grid;
      grid-template-columns: 1.4fr 1fr 1fr 1.2fr;
      gap: 34px;
      margin-bottom: 34px;
    }

    footer img {
      width: 145px;
      margin-bottom: 14px;
      padding: 8px;
      border-radius: 12px;
      background: var(--white);
    }

    footer h4 {
      margin-bottom: 14px;
    }

    footer p,
    footer a,
    footer li {
      color: rgba(255, 255, 255, 0.76);
      font-size: 14px;
    }

    footer ul {
      display: grid;
      gap: 7px;
      list-style: none;
    }

    .copyright {
      padding-top: 20px;
      border-top: 1px solid rgba(255, 255, 255, 0.13);
      color: rgba(255, 255, 255, 0.56);
      font-size: 13px;
      text-align: center;
    }

    @media (max-width: 980px) {
      .topbar .container {
        justify-content: center;
      }

      .menu-btn {
        display: block;
      }

      nav {
        position: absolute;
        top: calc(100% + 10px);
        left: 18px;
        right: 18px;
        display: flex;
        flex-direction: column;
        align-items: stretch;
        gap: 6px;
        max-height: 0;
        padding: 0 12px;
        overflow: hidden;
        opacity: 0;
        transform: translateY(-10px) scale(0.98);
        pointer-events: none;
        border: 1px solid rgba(223, 231, 234, 0.9);
        border-radius: 22px;
        background: rgba(255, 255, 255, 0.96);
        backdrop-filter: blur(16px);
        box-shadow: 0 24px 55px rgba(29, 100, 118, 0.18);
        transition:
          max-height 0.38s cubic-bezier(0.22, 1, 0.36, 1),
          padding 0.28s ease,
          opacity 0.24s ease,
          transform 0.28s ease;
      }

      nav.open {
        max-height: 430px;
        padding: 12px;
        opacity: 1;
        transform: translateY(0) scale(1);
        pointer-events: auto;
      }

      nav a {
        padding: 13px 14px;
        border-radius: 14px;
        color: var(--dark);
        transition: background 0.22s ease, color 0.22s ease, transform 0.22s ease;
      }

      nav a::after {
        display: none;
      }

      nav a:hover,
      nav a.active {
        background: linear-gradient(135deg, rgba(36, 123, 145, 0.10), rgba(36, 123, 145, 0.04));
        color: var(--blue);
        transform: translateX(3px);
      }

      .navbar > .btn {
        display: none;
      }

      .cards,
      .process-grid,
      .trust-strip-box {
        grid-template-columns: 1fr;
      }

      .trust-item {
        border-right: 0;
        border-bottom: 1px solid rgba(255, 255, 255, 0.16);
      }

      .trust-item:last-child {
        border-bottom: 0;
      }

      .benefit-grid {
        grid-template-columns: 1fr 1fr;
      }

      .benefit:nth-child(2) {
        border-right: 0;
      }

      .benefit:nth-child(1),
      .benefit:nth-child(2) {
        border-bottom: 1px solid var(--border);
      }

      .request-grid,
      .footer-grid {
        grid-template-columns: 1fr;
      }

      .hero {
        background-position: center;
      }

      .about-panel {
        grid-template-columns: 1fr;
      }

      .about-contact {
        border-left: 0;
        border-top: 1px solid var(--border);
      }

      .brand img {
        width: 100px;
      }
    }

    @media (max-width: 620px) {
      .topbar {
        display: none;
      }

      .navbar {
        height: 76px;
      }

      .brand-text {
        display: none;
      }

      .container {
        width: min(100% - 26px, 1180px);
      }

      .hero {
        min-height: auto;
      }

      .hero-content {
        padding: 62px 0;
      }

      .hero-actions .btn {
        width: 100%;
      }

      .cards,
      .process-grid,
      .form-grid {
        grid-template-columns: 1fr;
      }

      .benefit-grid {
        grid-template-columns: 1fr;
      }

      .benefit {
        min-height: auto;
        padding: 26px 22px;
        border-right: 0;
        border-bottom: 1px solid var(--border);
      }

      .benefit:last-child {
        border-bottom: 0;
      }

      .benefit:nth-child(2) {
        border-right: 0;
      }

      .benefit:nth-child(1),
      .benefit:nth-child(2) {
        border-bottom: 1px solid var(--border);
      }

      section {
        padding: 64px 0;
      }

      .about-image-side {
        min-height: auto;
        padding: 42px 24px;
      }

      .about-contact {
        padding: 34px 24px;
      }

      .about-contact .btn {
        width: 100%;
      }

      form {
        padding: 22px;
      }

      .trust-item {
        grid-template-columns: 58px 1fr;
        gap: 18px;
        padding: 28px 22px;
      }

      .trust-icon {
        width: 48px;
        height: 48px;
      }

      .trust-number {
        font-size: 34px;
      }
    }
  

    .contact-hero {
      position: relative;
      overflow: hidden;
      padding: 96px 0 82px;
      background:
        linear-gradient(90deg, rgba(255,255,255,0.97), rgba(255,255,255,0.86), rgba(255,255,255,0.58)),
        url("https://images.unsplash.com/photo-1497366754035-f200968a6e72?auto=format&fit=crop&w=1800&q=80") center / cover no-repeat;
    }

    .contact-hero-content {
      width: min(720px, 100%);
    }

    .contact-hero h1 {
      margin-bottom: 18px;
      color: var(--dark);
      font-size: clamp(42px, 6vw, 72px);
      line-height: 1.04;
      letter-spacing: -0.05em;
    }

    .contact-hero h1 span {
      color: var(--blue);
    }

    .contact-hero p {
      max-width: 650px;
      color: #394952;
      font-size: 19px;
    }

    .contact-main {
      background: linear-gradient(135deg, #ffffff 0%, #edf7f9 100%);
    }

    .contact-layout {
      display: grid;
      grid-template-columns: 0.85fr 1.15fr;
      gap: 34px;
      align-items: start;
    }

    .contact-info-panel,
    .contact-form-panel,
    .contact-note {
      border: 1px solid var(--border);
      border-radius: var(--radius);
      background: var(--white);
      box-shadow: var(--shadow);
    }

    .contact-info-panel {
      padding: 32px;
    }

    .contact-info-panel h2,
    .contact-form-panel h2 {
      margin-bottom: 12px;
      color: var(--dark);
      font-size: 30px;
      line-height: 1.15;
      letter-spacing: -0.025em;
    }

    .contact-info-panel > p {
      margin-bottom: 24px;
      color: #53616a;
    }

    .contact-methods {
      display: grid;
      gap: 14px;
      margin-bottom: 24px;
    }

    .contact-method {
      display: grid;
      grid-template-columns: 48px 1fr;
      gap: 14px;
      align-items: center;
      padding: 16px;
      border: 1px solid var(--border);
      border-radius: 16px;
      background: #fbfdfe;
    }

    .contact-method b {
      width: 48px;
      height: 48px;
      display: grid;
      place-items: center;
      border-radius: 14px;
      background: rgba(36, 123, 145, 0.11);
      color: var(--blue);
      font-size: 20px;
    }

    .contact-method strong {
      display: block;
      color: var(--dark);
      font-size: 16px;
      line-height: 1.3;
    }

    .contact-method a,
    .contact-method span {
      color: #5f6b72;
      font-size: 14px;
      font-weight: 600;
    }

    .contact-note {
      padding: 22px;
      background: linear-gradient(135deg, var(--dark), #17232b);
      color: var(--white);
    }

    .contact-note strong {
      display: block;
      margin-bottom: 8px;
      color: #9bd0dc;
      font-size: 18px;
    }

    .contact-note p {
      color: rgba(255,255,255,0.78);
      font-size: 14px;
      line-height: 1.7;
    }

    .contact-form-panel {
      padding: 32px;
    }

    .contact-form-panel > p {
      margin-bottom: 22px;
      color: #53616a;
    }

    .contact-page-form {
      padding: 0;
      border: 0;
      border-radius: 0;
      box-shadow: none;
      background: transparent;
    }

    .service-options {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 12px;
      margin-top: 8px;
    }

    .service-option {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 12px 13px;
      border: 1px solid #d4e0e4;
      border-radius: 12px;
      color: #34434c;
      font-weight: 700;
      cursor: pointer;
      background: #fbfdfe;
    }

    .service-option input {
      width: auto;
      accent-color: var(--blue);
    }

    .contact-map {
      padding-top: 0;
      background: var(--white);
    }

    .map-card {
      min-height: 280px;
      display: grid;
      place-items: center;
      padding: 34px;
      border: 1px dashed rgba(36, 123, 145, 0.35);
      border-radius: 28px;
      background:
        linear-gradient(135deg, rgba(36, 123, 145, 0.08), rgba(255,255,255,0.92)),
        repeating-linear-gradient(45deg, rgba(36,123,145,0.05) 0 12px, transparent 12px 24px);
      text-align: center;
    }

    .map-card h2 {
      margin-bottom: 10px;
      color: var(--dark);
      font-size: clamp(28px, 4vw, 42px);
      letter-spacing: -0.035em;
    }

    .map-card p {
      max-width: 640px;
      color: #53616a;
    }

    @media (max-width: 980px) {
      .contact-layout,
      .service-options {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 620px) {
      .contact-hero {
        padding: 64px 0 58px;
      }

      .contact-info-panel,
      .contact-form-panel {
        padding: 24px;
      }
    }

  </style>

  <style>
    /* ===== FINAL DESIGN POLISH: coherent blue corporate look + clean mobile layout ===== */
    :root {
      --blue: #0072d5;
      --blue-dark: #003488;
      --dark: #031a5b;
      --text: #10233f;
      --muted: #5d718b;
      --light: #eef7ff;
      --white: #ffffff;
      --border: #d3e9ff;
      --shadow: 0 18px 45px rgba(0, 80, 178, 0.16);
      --radius: 22px;
      --soft-blue: #f4faff;
      --accent: #56c2ff;
    }

    body {
      color: var(--text);
      background:
        radial-gradient(circle at 8% 0%, rgba(0, 114, 213, 0.08), transparent 30%),
        linear-gradient(180deg, #ffffff 0%, #f7fbff 100%);
    }

    .topbar {
      background: linear-gradient(135deg, var(--dark), #021446);
      box-shadow: 0 10px 30px rgba(3, 26, 91, 0.12);
    }

    header {
      background: rgba(255, 255, 255, 0.96);
      border-bottom: 1px solid rgba(211, 233, 255, 0.95);
      box-shadow: 0 8px 30px rgba(0, 80, 178, 0.07);
    }

    .navbar {
      height: 86px;
    }

    .brand {
      min-width: 0;
    }

    .brand img {
      width: 112px;
      padding: 4px;
      border-radius: 14px;
      background: #fff;
    }

    .brand-text strong {
      color: var(--dark);
      font-size: clamp(14px, 1.7vw, 20px);
      letter-spacing: 0.015em;
      white-space: nowrap;
    }

    .brand-text small {
      color: var(--blue);
      font-size: clamp(8px, 1vw, 11px);
      letter-spacing: 0.035em;
      white-space: nowrap;
    }

    nav a {
      color: #243650;
    }

    nav a:hover,
    nav a.active {
      color: var(--blue);
    }

    nav a::after {
      background: linear-gradient(90deg, var(--blue-dark), var(--blue), var(--accent));
    }

    .btn {
      border-radius: 14px;
      letter-spacing: -0.01em;
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--blue-dark), var(--blue) 62%, #0089ec);
      box-shadow: 0 14px 30px rgba(0, 114, 213, 0.25);
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 18px 38px rgba(0, 80, 178, 0.28);
    }

    .contact-hero {
      min-height: 520px;
      display: flex;
      align-items: center;
      padding: 96px 0 104px;
      isolation: isolate;
      background:
        radial-gradient(circle at 80% 22%, rgba(86, 194, 255, 0.22), transparent 26%),
        linear-gradient(90deg,
          rgba(255, 255, 255, 0.99) 0%,
          rgba(255, 255, 255, 0.94) 42%,
          rgba(238, 247, 255, 0.73) 62%,
          rgba(255, 255, 255, 0.22) 100%
        ),
        url("https://images.unsplash.com/photo-1497366754035-f200968a6e72?auto=format&fit=crop&w=1800&q=80") center right / cover no-repeat;
    }

    .contact-hero::after {
      content: "";
      position: absolute;
      inset: auto -8% -1px;
      height: 110px;
      z-index: -1;
      background:
        radial-gradient(80% 140% at 50% 100%, rgba(0, 114, 213, 0.18), transparent 62%),
        linear-gradient(180deg, transparent 0%, #f4faff 75%);
      clip-path: ellipse(72% 48% at 50% 100%);
      pointer-events: none;
    }

    .contact-hero-content {
      position: relative;
      z-index: 1;
      max-width: 720px;
      padding: 4px 0;
    }

    .eyebrow {
      color: var(--blue);
    }

    .contact-hero h1 {
      color: var(--dark);
      font-size: clamp(42px, 6.4vw, 76px);
      line-height: 0.99;
      letter-spacing: -0.06em;
      max-width: 700px;
    }

    .contact-hero h1 span {
      color: var(--blue);
    }

    .contact-hero p {
      color: #263846;
      font-weight: 600;
      line-height: 1.75;
    }

    .contact-main {
      padding: 78px 0 88px;
      background:
        radial-gradient(circle at 16% 12%, rgba(0, 114, 213, 0.08), transparent 26%),
        linear-gradient(135deg, #ffffff 0%, #eef7ff 100%);
    }

    .contact-layout {
      grid-template-columns: minmax(300px, 0.86fr) minmax(0, 1.14fr);
      gap: 36px;
    }

    .contact-info-panel,
    .contact-form-panel {
      border: 1px solid rgba(211, 233, 255, 0.95);
      border-radius: 28px;
      background: rgba(255, 255, 255, 0.94);
      box-shadow: 0 22px 55px rgba(0, 80, 178, 0.14);
      backdrop-filter: blur(10px);
    }

    .contact-info-panel {
      position: sticky;
      top: 112px;
      padding: 34px;
      overflow: hidden;
    }

    .contact-info-panel::before,
    .contact-form-panel::before {
      content: "";
      display: block;
      height: 5px;
      margin: -34px -34px 28px;
      background: linear-gradient(90deg, var(--blue-dark), var(--blue), var(--accent));
    }

    .contact-form-panel::before {
      margin: -34px -34px 28px;
    }

    .contact-form-panel {
      padding: 34px;
      overflow: hidden;
    }

    .contact-info-panel h2,
    .contact-form-panel h2 {
      color: var(--dark);
      font-size: clamp(26px, 3vw, 36px);
      letter-spacing: -0.04em;
    }

    .contact-info-panel > p,
    .contact-form-panel > p {
      color: var(--muted);
      line-height: 1.75;
    }

    .contact-methods {
      gap: 16px;
    }

    .contact-method {
      grid-template-columns: 52px 1fr;
      gap: 16px;
      padding: 17px;
      border: 1px solid rgba(211, 233, 255, 0.95);
      border-radius: 18px;
      background: linear-gradient(135deg, #ffffff, #f4faff);
      box-shadow: 0 10px 24px rgba(0, 80, 178, 0.07);
      transition: transform 0.22s ease, box-shadow 0.22s ease, border-color 0.22s ease;
    }

    .contact-method:hover {
      transform: translateY(-2px);
      border-color: rgba(0, 114, 213, 0.34);
      box-shadow: 0 14px 30px rgba(0, 80, 178, 0.12);
    }

    .contact-method b {
      width: 52px;
      height: 52px;
      border-radius: 16px;
      background: linear-gradient(135deg, rgba(0, 114, 213, 0.13), rgba(86, 194, 255, 0.18));
      color: var(--blue);
    }

    .contact-method strong {
      color: var(--dark);
    }

    .contact-method a,
    .contact-method span {
      color: #50647d;
      overflow-wrap: anywhere;
    }

    .contact-note {
      border: 0;
      border-radius: 22px;
      background:
        radial-gradient(circle at 90% 0%, rgba(86, 194, 255, 0.28), transparent 35%),
        linear-gradient(135deg, var(--dark), #021446);
      box-shadow: 0 18px 40px rgba(3, 26, 91, 0.18);
    }

    .contact-note strong {
      color: #8ed8ff;
    }

    .form-grid {
      gap: 18px;
    }

    label {
      color: #233755;
      font-weight: 850;
    }

    input,
    textarea,
    select {
      border: 1px solid #cfe5fb;
      border-radius: 14px;
      background: #fbfdff;
      color: var(--text);
      padding: 14px 15px;
      transition: border-color 0.22s ease, box-shadow 0.22s ease, background 0.22s ease;
    }

    input:hover,
    textarea:hover,
    select:hover {
      border-color: #a9d5fb;
      background: #ffffff;
    }

    input:focus,
    textarea:focus,
    select:focus {
      border-color: var(--blue);
      box-shadow: 0 0 0 4px rgba(0, 114, 213, 0.12);
      background: #ffffff;
    }

    textarea {
      min-height: 135px;
      line-height: 1.6;
    }

    .service-options {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 12px;
    }

    .service-option {
      min-height: 58px;
      align-items: flex-start;
      gap: 11px;
      padding: 14px;
      border: 1px solid #cfe5fb;
      border-radius: 16px;
      background: linear-gradient(135deg, #ffffff, #f6fbff);
      color: #243650;
      line-height: 1.35;
      transition: transform 0.2s ease, border-color 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
    }

    .service-option:hover {
      transform: translateY(-1px);
      border-color: rgba(0, 114, 213, 0.38);
      box-shadow: 0 10px 22px rgba(0, 80, 178, 0.09);
    }

    .service-option:has(input:checked) {
      border-color: var(--blue);
      background: linear-gradient(135deg, rgba(0, 114, 213, 0.12), rgba(86, 194, 255, 0.14));
      box-shadow: 0 10px 24px rgba(0, 80, 178, 0.11);
      color: var(--dark);
    }

    .service-option input {
      margin-top: 3px;
      accent-color: var(--blue);
      transform: scale(1.08);
    }

    .contact-page-form .btn {
      min-height: 54px;
      margin-top: 22px;
      border-radius: 16px;
      font-size: 16px;
    }

    footer {
      background:
        radial-gradient(circle at 84% 8%, rgba(86, 194, 255, 0.20), transparent 28%),
        linear-gradient(135deg, var(--dark), #021446);
    }

    footer img {
      width: 132px;
    }

    @media (max-width: 980px) {
      .navbar {
        height: 82px;
      }

      .brand img {
        width: 94px;
      }

      .brand-text strong {
        font-size: 14px;
      }

      .brand-text small {
        font-size: 8px;
      }

      nav {
        background: rgba(255, 255, 255, 0.98);
        border-color: rgba(211, 233, 255, 0.95);
        box-shadow: 0 24px 55px rgba(0, 80, 178, 0.18);
      }

      nav a:hover,
      nav a.active {
        background: linear-gradient(135deg, rgba(0, 114, 213, 0.11), rgba(86, 194, 255, 0.08));
        color: var(--blue);
      }

      .contact-layout,
      .service-options {
        grid-template-columns: 1fr;
      }

      .contact-info-panel {
        position: relative;
        top: auto;
      }
    }

    @media (max-width: 620px) {
      .topbar {
        display: none;
      }

      .container {
        width: min(100% - 26px, 1180px);
      }

      .navbar {
        height: 76px;
        gap: 8px;
      }

      .brand {
        flex: 1 1 auto;
        gap: 8px;
      }

      .brand img {
        width: clamp(58px, 18vw, 76px);
      }

      .brand-text {
        display: block !important;
        flex: 1 1 auto;
        min-width: 0;
        max-width: calc(100vw - 118px);
      }

      .brand-text strong {
        font-size: clamp(11px, 3.25vw, 14px);
        line-height: 1.08;
        letter-spacing: 0;
        white-space: nowrap;
      }

      .brand-text small {
        margin-top: 3px;
        font-size: clamp(6px, 1.85vw, 8px);
        line-height: 1;
        letter-spacing: 0;
        white-space: nowrap;
      }

      .menu-btn {
        width: 44px;
        height: 44px;
        flex: 0 0 44px;
        border-color: rgba(0, 114, 213, 0.18);
        border-radius: 14px;
      }

      .menu-icon,
      .menu-icon::before,
      .menu-icon::after {
        background: var(--dark);
      }

      .menu-btn.is-open {
        background: linear-gradient(135deg, var(--blue-dark), var(--blue));
      }

      .menu-btn.is-open .menu-icon::before,
      .menu-btn.is-open .menu-icon::after {
        background: #ffffff;
      }

      .contact-hero {
        min-height: calc(100svh - 76px);
        padding: 0;
        align-items: stretch;
        background:
          linear-gradient(90deg,
            rgba(255,255,255,0.98) 0%,
            rgba(255,255,255,0.94) 48%,
            rgba(255,255,255,0.57) 76%,
            rgba(255,255,255,0.18) 100%
          ),
          url("https://images.unsplash.com/photo-1497366754035-f200968a6e72?auto=format&fit=crop&w=1100&q=80") 66% center / cover no-repeat;
      }

      .contact-hero .container {
        min-height: calc(100svh - 76px);
        display: flex;
        align-items: center;
      }

      .contact-hero-content {
        max-width: 315px;
        padding: 42px 0 74px;
      }

      .contact-hero h1 {
        max-width: 310px;
        margin-bottom: 14px;
        font-size: clamp(32px, 10.5vw, 44px);
        line-height: 1.02;
        text-shadow: 0 4px 18px rgba(255,255,255,0.98);
      }

      .contact-hero p {
        max-width: 285px;
        font-size: 14px;
        line-height: 1.62;
        font-weight: 650;
        text-shadow: 0 2px 14px rgba(255,255,255,0.96);
      }

      .contact-main {
        padding: 54px 0 62px;
      }

      .contact-layout {
        gap: 20px;
      }

      .contact-info-panel,
      .contact-form-panel {
        padding: 22px;
        border-radius: 22px;
      }

      .contact-info-panel::before,
      .contact-form-panel::before {
        height: 4px;
        margin: -22px -22px 22px;
      }

      .contact-info-panel h2,
      .contact-form-panel h2 {
        font-size: 25px;
      }

      .contact-method {
        grid-template-columns: 46px 1fr;
        padding: 14px;
        border-radius: 16px;
      }

      .contact-method b {
        width: 46px;
        height: 46px;
      }

      .form-grid {
        grid-template-columns: 1fr;
        gap: 15px;
      }

      input,
      textarea,
      select {
        min-height: 48px;
        font-size: 16px;
      }

      .service-options {
        grid-template-columns: 1fr;
      }

      .service-option {
        min-height: 52px;
        padding: 13px;
      }

      .contact-page-form .btn {
        width: 100%;
        min-height: 52px;
      }

      footer {
        padding-top: 46px;
      }
    }

    @media (max-width: 380px) {
      .contact-hero-content {
        max-width: 286px;
      }

      .contact-hero h1 {
        font-size: clamp(29px, 10vw, 39px);
      }

      .contact-hero p {
        max-width: 260px;
        font-size: 13px;
      }
    }


    /* Privacy consent checkbox */
    .privacy-consent {
      display: grid;
      grid-template-columns: 22px 1fr;
      gap: 12px;
      align-items: flex-start;
      margin-top: 22px;
      padding: 16px 18px;
      border: 1px solid rgba(0, 114, 213, 0.18);
      border-radius: 16px;
      background: linear-gradient(135deg, rgba(238, 247, 255, 0.78), rgba(255, 255, 255, 0.96));
      color: #34434c;
      box-shadow: 0 10px 26px rgba(0, 80, 178, 0.08);
    }

    .privacy-consent input {
      width: 18px;
      height: 18px;
      margin-top: 3px;
      accent-color: var(--blue);
      cursor: pointer;
    }

    .privacy-consent label {
      margin: 0;
      color: #34434c;
      font-size: 13px;
      font-weight: 650;
      line-height: 1.55;
      cursor: pointer;
    }

    .privacy-consent a {
      color: var(--blue);
      font-weight: 900;
      text-decoration: underline;
      text-underline-offset: 3px;
    }

    .privacy-consent small {
      display: block;
      margin-top: 5px;
      color: var(--muted);
      font-size: 12px;
      font-weight: 600;
      line-height: 1.45;
    }

    @media (max-width: 620px) {
      .privacy-consent {
        grid-template-columns: 20px 1fr;
        gap: 10px;
        margin-top: 18px;
        padding: 14px;
        border-radius: 14px;
      }

      .privacy-consent label {
        font-size: 12.5px;
      }
    }
  </style>


  <style>
    /* Smooth scroll reveal enhancement */
    .smooth-reveal,
    .soft-reveal {
      opacity: 0;
      transform: translateY(28px);
      transition: opacity 0.8s ease, transform 0.8s ease;
      will-change: opacity, transform;
    }

    .smooth-reveal.is-visible,
    .soft-reveal.is-visible {
      opacity: 1;
      transform: translateY(0);
    }

    @media (prefers-reduced-motion: reduce) {
      .smooth-reveal,
      .soft-reveal {
        opacity: 1 !important;
        transform: none !important;
        transition: none !important;
      }
    }
  </style>

</head>
<body>
  <div class="topbar">
    <div class="container">
      <span>✉ info@bhv-immobilienverwaltung.de</span>
      <span>☎ 0176 1087 0618</span>
    </div>
  </div>

  <header>
    <div class="container navbar">
      <a class="brand" href="index.html#start" aria-label="BHV Immobilienverwaltung GmbH">
        <img src="assets/logo.png" alt="BHV Immobilienverwaltung GmbH Logo" />
        <span class="brand-text">
          <strong>Hausverwaltung Böhme</strong>
          <small>WEG-Verwaltung · Sondereigentumsverwaltung · Mietverwaltung</small>
        </span>
      </a>

      <nav id="menu">
        <a href="index.html#start">Start</a>
        <a href="index.html#leistungen">Leistungen</a>
        <a href="index.html#ablauf">Ablauf</a>
        <a href="index.html#vorteile">Vorteile</a>
        <a class="active" href="#kontakt">Kontakt</a>
        <a href="index.html#ueber-uns">Über uns</a>
      </nav>

      <a href="#kontakt" class="btn btn-primary">Anfrage stellen</a>

      <button
        class="menu-btn"
        type="button"
        aria-label="Menü öffnen"
        aria-expanded="false"
        aria-controls="menu"
        onclick="toggleMenu(this)"
      >
        <span class="menu-icon" aria-hidden="true"></span>
      </button>
    </div>
  </header>

  
  <main>
    <section class="contact-hero" id="kontakt">
      <div class="container">
        <div class="contact-hero-content soft-reveal is-visible">
          <p class="eyebrow">Kontakt</p>
          <h1>Wir freuen uns auf <span>Ihre Anfrage</span></h1>
          <p>
            Schreiben Sie uns kurz, um welche Immobilie es geht. Wir melden uns
            schnellstmöglich bei Ihnen zurück und besprechen die nächsten Schritte persönlich.
          </p>
        </div>
      </div>
    </section>

    <section class="contact-main">
      <div class="container contact-layout">
        <aside class="contact-info-panel">
          <p class="eyebrow">BHV Immobilienverwaltung</p>
          <h2>Direkt erreichbar</h2>
          <p>
            Für WEG-Verwaltung, Sondereigentumsverwaltung und Mietverwaltung stehen wir Ihnen
            zuverlässig zur Seite.
          </p>

          <div class="contact-methods">
            <div class="contact-method">
              <b>✉</b>
              <div>
                <strong>E-Mail</strong>
                <a href="mailto:info@bhv-immobilienverwaltung.de">info@bhv-immobilienverwaltung.de</a>
              </div>
            </div>

            <div class="contact-method">
              <b>☎</b>
              <div>
                <strong>Telefon</strong>
                <a href="tel:+4917610870618">0176 1087 0618</a>
              </div>
            </div>
          </div>

          <div class="contact-note">
            <strong>Schnelle Rückmeldung</strong>
            <p>
              Je genauer Ihre Angaben zur Immobilie sind, desto schneller können wir Ihren
              Verwaltungsbedarf einschätzen und ein passendes Angebot vorbereiten.
            </p>
          </div>
        </aside>

        <div class="contact-form-panel">
          <p class="eyebrow">Anfrage senden</p>
          <h2>Unverbindliches Angebot anfordern</h2>
          <p>
            Füllen Sie das Formular aus. Beim Absenden wird Ihre E-Mail-App mit allen Angaben geöffnet.
          </p>

          <form class="contact-page-form" onsubmit="sendMail(event)">
            <div class="form-grid">
              <div>
                <label for="name">Name</label>
                <input id="name" name="name" placeholder="Ihr Name" required />
              </div>

              <div>
                <label for="email">E-Mail</label>
                <input id="email" name="email" type="email" placeholder="Ihre E-Mail-Adresse" required />
              </div>

              <div>
                <label for="phone">Telefon</label>
                <input id="phone" name="phone" type="tel" placeholder="Telefonnummer" />
              </div>

              <div>
                <label for="units">Einheiten</label>
                <input id="units" name="units" type="number" min="1" placeholder="z. B. 12" />
              </div>

              <div class="full">
                <label for="object">Objekt / Ort</label>
                <select id="object" name="object" onchange="toggleCustomObject(this)">
                  <option value="" selected disabled>Bitte auswählen</option>
                  <option value="1 Objekt">1 Objekt</option>
                  <option value="2 Objekte">2 Objekte</option>
                  <option value="3 Objekte">3 Objekte</option>
                  <option value="4 Objekte">4 Objekte</option>
                  <option value="5+ Objekte">5+ Objekte</option>
                  <option value="Sonstiges">Sonstiges / Eigene Eingabe</option>
                </select>
                <input
                  class="custom-object-field"
                  id="objectCustom"
                  name="objectCustom"
                  placeholder="Eigene Angabe schreiben..."
                  style="display: none; margin-top: 12px;"
                />
              </div>

              <div class="full">
                <label>Gewünschte Leistung</label>
                <div class="service-options">
                  <label class="service-option"><input type="checkbox" name="services" value="WEG-Verwaltung" /> WEG-Verwaltung</label>
                  <label class="service-option"><input type="checkbox" name="services" value="Sondereigentumsverwaltung" /> Sondereigentumsverwaltung</label>
                  <label class="service-option"><input type="checkbox" name="services" value="Mietverwaltung" /> Mietverwaltung</label>
                </div>
              </div>

              <div class="full">
                <label for="message">Nachricht</label>
                <textarea id="message" name="message" placeholder="Beschreiben Sie kurz Ihr Anliegen..."></textarea>
              </div>
            </div>

            <div class="privacy-consent">
              <input id="privacy" name="privacy" type="checkbox" required />
              <label for="privacy">
                Ich habe die <a href="datenschutz.html" target="_blank" rel="noopener">Datenschutzerklärung</a> gelesen und stimme zu, dass meine Angaben zur Bearbeitung meiner Anfrage verwendet werden.
                <small>Die Einwilligung kann jederzeit für die Zukunft widerrufen werden.</small>
              </label>
            </div>

            <button class="btn btn-primary" type="submit">Anfrage per E-Mail senden →</button>
          </form>
        </div>
      </div>
    </section>

  </main>
  <footer>
    <div class="container">
      <div class="footer-grid">
        <div>
          <img src="assets/logo.png" alt="BHV Logo" />
          <p>BHV Immobilienverwaltung GmbH – zuverlässig, transparent und persönlich.</p>
        </div>

        <div>
          <h4>Navigation</h4>
          <ul>
            <li><a href="index.html#start">Start</a></li>
            <li><a href="index.html#leistungen">Leistungen</a></li>
            <li><a href="#kontakt">Kontakt</a></li>
            <li><a href="index.html#ueber-uns">Über uns</a></li>
          </ul>
        </div>

        <div>
          <h4>Leistungen</h4>
          <ul>
            <li>WEG-Verwaltung</li>
            <li>Sondereigentumsverwaltung</li>
            <li>Mietverwaltung</li>
            <li>Objektbetreuung</li>
          </ul>
        </div>

        <div>
          <h4>Kontakt</h4>
          <ul>
            <li>
              <a href="mailto:info@bhv-immobilienverwaltung.de">
                info@bhv-immobilienverwaltung.de
              </a>
            </li>
            <li><a href="tel:+4917610870618">0176 1087 0618</a></li>
          </ul>
        </div>
      </div>

      <div class="copyright">
        © 2026 BHV Immobilienverwaltung GmbH. Alle Rechte vorbehalten.
      </div>
    </div>
  </footer>


  <script>
    function toggleMenu(button) {
      const menu = document.getElementById("menu");
      const isOpen = menu.classList.toggle("open");

      button.classList.toggle("is-open", isOpen);
      button.setAttribute("aria-expanded", isOpen ? "true" : "false");
      button.setAttribute("aria-label", isOpen ? "Menü schließen" : "Menü öffnen");
    }

    document.addEventListener("click", (event) => {
      const menu = document.getElementById("menu");
      const button = document.querySelector(".menu-btn");

      if (!menu || !button || !menu.classList.contains("open")) return;
      if (menu.contains(event.target) || button.contains(event.target)) return;

      menu.classList.remove("open");
      button.classList.remove("is-open");
      button.setAttribute("aria-expanded", "false");
      button.setAttribute("aria-label", "Menü öffnen");
    });

    document.addEventListener("keydown", (event) => {
      if (event.key !== "Escape") return;

      const menu = document.getElementById("menu");
      const button = document.querySelector(".menu-btn");

      if (!menu || !button) return;

      menu.classList.remove("open");
      button.classList.remove("is-open");
      button.setAttribute("aria-expanded", "false");
      button.setAttribute("aria-label", "Menü öffnen");
    });

        function toggleCustomObject(select) {
      const customField = document.getElementById("objectCustom");
      if (!customField) return;

      if (select.value === "Sonstiges") {
        customField.style.display = "block";
        customField.focus();
      } else {
        customField.style.display = "none";
        customField.value = "";
      }
    }

    function sendMail(event) {
      event.preventDefault();

      const form = event.target;

      if (!form.privacy.checked) {
        form.privacy.focus();
        return;
      }

      const services = Array.from(form.querySelectorAll('input[name="services"]:checked'))
        .map((input) => input.value)
        .join(', ');

      const objectValue = form.object.value === "Sonstiges"
        ? (form.objectCustom.value || "Sonstiges")
        : form.object.value;

      const subject = encodeURIComponent("Neue Kontaktanfrage über die BHV Website");
      const body = encodeURIComponent(
        `Name: ${form.name.value}\n` +
        `E-Mail: ${form.email.value}\n` +
        `Telefon: ${form.phone.value}\n` +
        `Objekt / Ort: ${objectValue || 'Keine Auswahl'}\n` +
        `Einheiten: ${form.units.value}\n` +
        `Gewünschte Leistungen: ${services || 'Keine Auswahl'}\n` +
        `Datenschutz: Zustimmung erteilt\n\n` +
        `Nachricht:\n${form.message.value}`
      );

      window.location.href =
        `mailto:info@bhv-immobilienverwaltung.de?subject=${subject}&body=${body}`;
    }

    function revealPage() {
      document.body.classList.add("page-ready");

      const items = document.querySelectorAll("section, .contact-method, .contact-form-panel, .contact-note, footer");
      items.forEach((item) => item.classList.add("soft-reveal"));

      const observer = new IntersectionObserver(
        (entries, obs) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add("is-visible");
              obs.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.12 }
      );

      items.forEach((item) => observer.observe(item));
    }

    revealPage();
  </script>
</body>
</html>
